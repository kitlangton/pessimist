require 'spec_helper'

describe Pessimist do
  it 'has a version number' do
    expect(Pessimist::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
